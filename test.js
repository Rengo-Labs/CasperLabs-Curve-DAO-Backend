// const { ACCOUNT_HASH_ID } = require("casper-js-sdk");

// let A="100",B="200";  //models, args (strings)

// let z=BigInt(A)+BigInt(B); //convert them into BitInt for mathematical operations

// console.log("z: ",z.toString());

// let blockNumber="12667824";
// //ACCOUNT.z=z.toString(); // convert to string
// if (BigInt(args.blockNumber) >= BigInt("12667823"))
// {
//     console.log("greater");
// }

